# Deprecated migrations

Files in this folder are **not** executed by `php artisan migrate` or Docker init scripts.

They are kept for historical reference only. Superseded by numbered migrations in the parent directory.
